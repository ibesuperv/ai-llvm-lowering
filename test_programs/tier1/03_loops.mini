fn sum_to_n(n: int): int {
    let total: int = 0;
    let i: int = 1;
    while (i <= n) {
        total = total + i;
        i = i + 1;
    }
    return total;
}

fn main(): int {
    let sum: int = sum_to_n(10);
    print(sum);
    return 0;
}
