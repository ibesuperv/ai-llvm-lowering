fn sum_to_n(n: int): int {
    let total: int = 0;
    for (let i: int = 1; i <= n; i = i + 1) {
        total = total + i;
    }
    return total;
}

fn main(): int {
    let result: int = sum_to_n(10);
    print(result);
    return 0;
}
